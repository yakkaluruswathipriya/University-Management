package university.management.system;import javax.swing.*;import java.awt.*;public class Splash extends JFrame implements Runnable {    Thread t;    Splash() {        setUndecorated(true);        setSize(1000, 700);        setLocationRelativeTo(null); // Centered        // Background panel with layout        JPanel content = new JPanel();        content.setLayout(new BorderLayout());        content.setBackground(Color.WHITE);        add(content);        // Add splash image        ImageIcon i1 = new ImageIcon(ClassLoader.getSystemResource("icon/first.png"));        Image i2 = i1.getImage().getScaledInstance(1000, 700, Image.SCALE_SMOOTH);        JLabel imageLabel = new JLabel(new ImageIcon(i2));        imageLabel.setLayout(new BorderLayout());        content.add(imageLabel, BorderLayout.CENTER);        // Add overlay text        JLabel title = new JLabel("SRM University Management System", SwingConstants.CENTER);        title.setFont(new Font("SansSerif", Font.BOLD, 30));        title.setForeground(Color.WHITE);        title.setBorder(BorderFactory.createEmptyBorder(20, 0, 50, 0));        imageLabel.add(title, BorderLayout.SOUTH);        // Optional progress bar        JProgressBar bar = new JProgressBar();        bar.setIndeterminate(true);        bar.setBorderPainted(false);        bar.setPreferredSize(new Dimension(0, 5));        bar.setForeground(new Color(100, 100, 255));        content.add(bar, BorderLayout.SOUTH);        setVisible(true);        // Start thread        t = new Thread(this);        t.start();    }    public void run() {        try {            Thread.sleep(5000); // 5 seconds display            setVisible(false);            new Login(); // Go to login screen        } catch (Exception e) {            e.printStackTrace();        }    }    public static void main(String[] args) {        SwingUtilities.invokeLater(Splash::new);    }}